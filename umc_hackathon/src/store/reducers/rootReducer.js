const initialState = {
  //초기 상태정의하기
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    //상태 업데이트 정의하기
    default:
      return state;
  }
};

export default rootReducer;
